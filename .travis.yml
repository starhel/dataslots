sudo: false

language: python

python:
  - 3.7-dev

install:
  - pip install pipenv
  - pip install codecov
  - pipenv install --dev --system

script:
  - nosetests tests --with-coverage

after_success:
  - codecov
