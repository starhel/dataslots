sudo: false

language: python

python:
  - 3.7-dev

install:
  - pip install pipenv
  - pip install codecov
  - pipenv install --dev --system

script:
  - nosetests tests --with-coverage --cover-package dataslots -cover-inclusive -v -a '!local-only'

after_success:
  - codecov
